# CLI-Inspired Portfolio

A minimal, interactive terminal-style portfolio built to explore my work in a clean CLI-inspired interface.
> "If I could introduce myself in a shell, it’d look like this."

